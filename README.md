##### Intended features



* help the user for first-use navigation
* sort returned contents of Path.iterdir()
* if pathname is too long, convert it to "path...name" format
* deny to access \& do some operations on $RecycleBin and other vital system paths in directories
* log every operations in a logbook using logging

===================================

bugs:

